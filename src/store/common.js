const state = {
}

const getters = {
}

const mutations = {
}

const actions = {
  refresh({ commit, dispatch }) {
  }
}

// Global module loaded on first app load.
export default {
  namespaced: true,
  state: { ...state },
  mutations,
  getters,
  actions
}
