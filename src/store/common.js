// Common State.
const state = {
}

const mutations = {
}

const getters = {
}

const actions = {
}

// Global module loaded on first app load.
export default {
  namespaced: true,
  state: Object.assign({}, state),
  mutations,
  getters,
  actions
}
